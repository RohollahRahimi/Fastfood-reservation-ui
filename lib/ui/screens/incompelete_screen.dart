import 'package:flutter/material.dart';

class IncompeleteScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text(
              "This screen is not compelete yet!\n\n It'll be complete in full project"),
        ),
      ),
    );
  }
}
